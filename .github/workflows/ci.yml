name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install Ruff
        run: pip install ruff

      - name: Run Ruff Linter
        run: ruff check . --output-format=github
        continue-on-error: true

      - name: Run Ruff Formatter Check
        run: ruff format --check .
        continue-on-error: true

  type-check:
    name: Type Consistency Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e . || true

      - name: Run Mypy
        run: |
          pip install mypy
          mypy spiderforge/ --ignore-missing-imports || true
        continue-on-error: true

  test:
    name: Test Suite (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11']
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install Package & Tests
        run: |
          python -m pip install --upgrade pip
          pip install pytest httpx fastapi typer rich pydantic
          pip install -e .

      - name: Run Pytest
        run: pytest -v --tb=short
        continue-on-error: true

  doctor-check:
    name: Doctor Smoke Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install Package
        run: |
          python -m pip install --upgrade pip
          pip install httpx typer rich pydantic
          pip install -e .

      - name: Run Doctor CLI Smoke Test
        run: python -m spiderforge.cli.main --help
