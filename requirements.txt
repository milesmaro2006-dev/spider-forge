# ═══════════════════════════════════════════════════════════════
#  SpiderForge — legacy install path
#  ═══════════════════════════════════════════════════════════════
#
#  ⚠️  الطريقة المُوصى بها هي:
#      pipx install .
#  أو:
#      pip install -e ".[all]"
#
#  الملف ده موجود للتوافق مع أدوات قديمة أو بيئات CI بسيطة.
#  المصدر الوحيد للحقيقة هو pyproject.toml — أي تعديل لازم
#  يتعمل هناك الأول.
# ═══════════════════════════════════════════════════════════════

# ─── Core runtime (مطابق لـ pyproject.toml [project] dependencies) ───
typer>=0.12.0
rich>=13.7.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
pydantic>=2.7.0
SQLAlchemy>=2.0.30
PyYAML>=6.0.1
Jinja2>=3.1.3
dnspython>=2.6.1

# ─── Web Dashboard (اختياري — للحصول عليه: pip install -e ".[web]") ───
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ─── PDF (اختياري — يحتاج system libs، شوف README) ───
# weasyprint>=62.0

# ─── Browser (اختياري — ثقيل جداً، مش مفعّل افتراضياً) ───
# playwright>=1.44.0
